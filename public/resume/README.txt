Place your two resume PDFs here with these exact filenames
(the site already links to these paths):

  resume-analyst.pdf   -> used by hero "Resume" CTA + contact section
  resume-software.pdf  -> referenced in lib/data.ts if you want to link
                          a second, SWE-focused resume from somewhere

If you only want one resume on the site, delete the unused reference
in lib/data.ts (profile.resumeSoftware) and any component using it.
